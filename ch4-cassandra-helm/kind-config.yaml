# three node cluster config
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  # extraMounts:
  # - hostPath: /Users/rico/.docker/daemon.json # 本地 docker daemon.json 文件路径
  #   containerPath: /etc/docker/daemon.json
- role: worker
- role: worker
- role: worker
